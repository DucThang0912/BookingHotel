@model ShoppingCart

<h2>Shopping Cart</h2>

@if (Model.Items.Count == 0)
{
    <p>Your shopping cart is empty.</p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Room</th>
                <th>Ngày Nhận</th>
                <th>Ngày trả</th>
                <th>Số người</th>
                <th>Price</th>
                <th>Quantity</th>
                <th>Total</th>
                <th></th>
            </tr>
        </thead>
        @* <tbody>
            @foreach (var item in Model.Items)
            {
                <tr>
                    <td>@item.Name</td>
                    <td>@item.CheckInDate</td>
                    <td>@item.CheckOutDate</td>
                    <td>@item.Adults người lớn | @item.Children trẻ em</td>
                    <td>@string.Format("{0:C0}", item.Price) VND</td> <!-- Định dạng giá và thêm VND -->
                    <td>@item.Quantity</td>
                    <td>@string.Format("{0:C0}", item.Price * item.Quantity) VND</td> <!-- Định dạng tổng cộng và thêm VND -->
                    <td>
                        <form asp-controller="ShoppingCart" asp-action="RemoveFromCart" method="post">
                            <input type="hidden" name="roomId" value="@item.Id" />
                            <button type="submit" class="btn btn-sm btn-danger">Remove</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody> *@
    </table>

   @*  <div class="text-right">
        <h4>Total: @string.Format("{0:C0}", Model.Items.Sum(i => i.Price * i.Quantity)) VND</h4> <!-- Tổng cộng và thêm VND -->
        <a class="btn btn-info" asp-action="Checkout">Thanh toán</a>
    </div> *@
}
